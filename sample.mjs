export class StudentDetails {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }
}

export class CarDetails {
  constructor(brand, price) {
    this.brand = brand;
    this.price = price;
  }
}
